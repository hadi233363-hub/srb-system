// Phase 2 — simulation disabled.
// The company system now runs on real manual data (Prisma + SQLite).
// If you ever want the simulation back for training/demos, re-enable:
//   import { ensureStarted } from "./lib/sim/engine";
//   ensureStarted();

export async function register() {
  // no-op — real mode
}
